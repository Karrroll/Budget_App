#ifndef TYPE_H_INCLUDED
#define TYPE_H_INCLUDED

enum class FieldType {
    FIRST_NAME,
    LAST_NAME,
    LOGIN,
    PASSWORD
};

enum class MenuType {
    MAIN_MENU,
    USER_MENU
};

#endif // TYPE_H_INCLUDED
